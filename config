# 如果你是第一次打开，请仔细阅读！！！
#
# 本文件负责对本次实验进行配置
# min_thread_num指定了本次实验的最小线程数
# max_thread_num指定了本次实验的最大线程数
# args是你编写的程序除去最后一个参数（即线程数参数）之外的前几个参数
# repeat是每个线程数下重复实验的次数
#
# 假设如下场景示例（纯属假设！仅作为生动形象的示例！）：
# 你的程序是program.c，它会依次向控制台读取n、step和np三个参数，其中np是执行此程序的线程数
# 编译好后的可执行文件为program，那么你肯定会像这样运行你的程序：
# ./program 100 100 4
# 很好!你编写的程序符合实验指导书的模板，因为程序的最后一个参数np标识了执行这个程序所用的线程数，那么你可以快乐地使用这个脚本
# 下面就给出了一种合法的config书写方式，最小线程是1，最大线程是16，args是100 100，重复实验10次
# 请注意args！！！不需要填写最后一个np参数！！！因为np参数在自动化实验中会变动！！！
# 运行脚本，那么脚本会向服务器提交160个任务
# 分别是1线程（也就是np传入1）的10次重复实验、2线程（也就是np传入2）的10次重复实验……一直到16线程的10次重复实验
#
# 场景示例描述完毕！请根据你的现实情况来改写这个config文件！
#
# 再次提醒！
# 请确保你的程序是正确的！
# 请仔细检查args的填写，如果你的程序需要n个参数，且第n个参数指定了程序运行的线程数，请只把前n-1个参数填到args里！
#
# 根据需要改写下面的config吧！

min_thread_num:1
max_thread_num:16
args:100 100
repeat:10